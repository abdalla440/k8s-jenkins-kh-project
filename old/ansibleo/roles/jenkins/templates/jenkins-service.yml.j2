apiVersion: v1
kind: Service
metadata:
  name: jenkins
  namespace: {{ jenkins_namespace }}
spec:
  type: {{ jenkins_service_type }}
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: jenkins
